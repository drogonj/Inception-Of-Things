apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: inception-of-things
  labels:
    app: app2
  annotations:
    description: "Deployment of app2 - a simple hello, world"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app2-container
        image: hashicorp/http-echo:latest
        args:
        - "-text=Hello from app2"
        imagePullPolicy: IfNotPresent
        # Ports
        ports:
        - name: http
          protocol: TCP
          containerPort: 5678
        # Resources usage
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        # HealthCheck
        livenessProbe:
          httpGet:
            path: /
            port: 5678
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 5678
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 1

---
apiVersion: v1
kind: Service
metadata:
  name: app2-service
  namespace: inception-of-things
spec:
  selector:
    app: app2
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5678